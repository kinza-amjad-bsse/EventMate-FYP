import 'package:flutter/material.dart';

class AppColors {
  static const white = Colors.white, black = Colors.black, grey = Colors.grey;
  static const primary = Color(0xFF800080);
  static const background = Color(0xFFfafafa);
  static const appTheme = Color(0xFF800080);
}
