import 'package:event_mate/Screens/SplashScreen/Widgets/widgets.dart';
import '../../Exporter/exporter.dart';

class SplashScreen extends StatelessWidget {
  const SplashScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SplashBody(),
    );
  }
}
